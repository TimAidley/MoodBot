version: '3'

services:
  mood-tracker:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - mood-data:/app/instance
    environment:
      - SUBJECT_NAME=Gabriel
    restart: unless-stopped

volumes:
  mood-data:
